import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-contact',
  templateUrl: './contact.component.html',
  styles: []
})
export class ContactComponent implements OnInit {

 
  constructor() { }

  ngOnInit() {
    
    //let dither=0.001;
    //this.latitude = this.latitude + (Math.random()-0.5)*dither;
    //this.longitude == this.longitude + (Math.random()-0.5)*dither;

  }

}
